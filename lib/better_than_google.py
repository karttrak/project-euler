import structures

def bfs(grid, start, target):

	return grid


if __name__ == '__main__':

	grid = [
			['S','0','0'],
			['0','0','0'],
			['0','0','F']
			]

	for line in grid:

		for cell in line:

			print(cell, end=' ')

		print()